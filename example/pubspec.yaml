name: fitness_example
description: Demonstrates how to use the fitness plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk:

dependencies:
  flutter:
    sdk: flutter
  fitness:
  intl:
  lottie:
  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
